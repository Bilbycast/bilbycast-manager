# bilbycast-manager configuration

# Server listen port
listen_port = 8443

# SQLite database URL (created automatically if it doesn't exist)
database_url = "sqlite:/Users/rezarahimi/Development/dev_claude/bilbycast-manager/bilbycast-manager.db?mode=rwc"

# JWT signing secret (CHANGE THIS in production!)
# Must be at least 32 characters
jwt_secret = "change-me-to-a-secure-random-secret-in-production"

# Master encryption key for stored secrets (API keys, node credentials)
# CHANGE THIS in production! Derive from a secure passphrase.
master_key = "change-me-to-a-secure-master-key-in-production"
